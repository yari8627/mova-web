"use client";
import { FormEvent, useState } from "react";
import { ArrowLeft, Mail } from "lucide-react";
import { useRouter } from "next/navigation";
export default function ForgotPasswordPage() { const router = useRouter(); const [message, setMessage] = useState(""); const [preview, setPreview] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const result = await fetch("/api/auth/forgot-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: data.get("email") }) }).then((response) => response.json()); setMessage(result.message); setPreview(result.previewUrl || ""); } return <main className="auth-shell"><button className="icon-button auth-back" onClick={() => router.push("/auth")}><ArrowLeft size={20} /></button><section className="auth-card"><div className="auth-brand">mova</div><h1>Recupera password</h1><p>Inserisci l’email associata al tuo account.</p><form onSubmit={submit}><label>Email<span><Mail size={18} /><input name="email" type="email" required /></span></label><button className="primary-button auth-submit">Invia istruzioni</button></form>{message && <div className="security-result">{message}{preview && <button onClick={() => router.push(preview)}>Apri link di sviluppo</button>}</div>}</section></main>; }
